{
  "name": "alchemy-mono",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "module": "./lib/index.js",
  "scripts": {
    "check": "tsc -b ./tsconfig.json",
    "publish:npm": "rm -rf ./lib && tsc -b && npm publish"
  },
  "workspaces": ["alchemy", "examples/*"],
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/bun": "latest",
    "@types/node": "latest",
    "typescript": "latest"
  }
}
